# Resolve Whisper - Windows / non-Mac dependencies
#
# Windows install is handled by setup.ps1, which installs torch first
# (CUDA build if an NVIDIA GPU is present, otherwise CPU) and then this file.
# The Windows backend is faster-whisper, which ships its own VAD, so the
# Mac-only silero-vad + mlx packages are not needed here.
#
# Manual install:
#   pip install torch            # or the CUDA wheel, see setup.ps1
#   pip install -r requirements.txt

faster-whisper>=1.0.0
numpy
