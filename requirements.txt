# Resolve Whisper - dependencies
#
# The setup scripts (setup.bat / setup.sh) handle platform-specific
# installs automatically. Use those for the recommended install path.
#
# Manual install (CLI --file mode only):
#
# Windows (CUDA):
#   pip install torch
#   pip install -r requirements.txt
#
# Windows (no NVIDIA GPU):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# Mac (Apple Silicon):
#   pip install -r requirements-mac.txt

# Core (Windows / Linux)
faster-whisper>=1.0.0
numpy
