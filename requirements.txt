# Resolve Whisper - Windows / non-Mac dependencies
#
# Windows install is handled by setup.ps1. On a machine with an NVIDIA GPU it
# also installs the CUDA build of PyTorch (which provides the cuda probe and
# bundles the cuBLAS/cuDNN DLLs CTranslate2 needs for GPU); CPU-only machines
# skip torch entirely. The Windows backend is faster-whisper, which ships its
# own VAD, so the Mac-only silero-vad + mlx packages are not needed here.
#
# Manual GPU install:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt

faster-whisper>=1.0.0
numpy
