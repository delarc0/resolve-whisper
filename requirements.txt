# Resolve Whisper - dependencies
#
# Mac (Apple Silicon) is the supported platform:
#   pip install -r requirements-mac.txt
#
# The entries below are only for running `caption.py --file` on a
# non-Mac machine (no Resolve integration there):
#   pip install torch
#   pip install -r requirements.txt

faster-whisper>=1.0.0
numpy
